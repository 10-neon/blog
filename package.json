{
  "devDependencies": {
    "vitepress": "^1.3.2"
  },
  "scripts": {
    "docs:dev": "node gen-sidebar.js && vitepress dev docs",
    "docs:build": "node gen-sidebar.js && vitepress build docs",
    "docs:preview": "node gen-sidebar.js && vitepress preview docs",
    "docs:sidebar": "node gen-sidebar.js"
  }
}